# sentinel-agents

一个由多个专职 Agent 组成的协作团队。

- **agent0** — 专注于信息采集、预处理与结构化存储，为后续 UI 或 Runtime 渲染提供标准化数据源。全流程可观测、可复现，生命周期事件完整记录。
- **agent1** — 独立完成红队攻击的闭环，并辅以生成蓝队策略提升。

## 目录结构

- `agent0/` — 数据采集与结构化存储
- `agent1/` — 红队攻击闭环 + 蓝队策略
- `docs/`   — 架构与设计文档
